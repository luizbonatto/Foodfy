{% extends 'layout.njk' %}

{% block head %}
    <title>Foodfy | Receitas</title>
{% endblock head %}

{% block header %}
            <a href="/"><img src="/images/logo.png" alt="Flo"></a>
            <a href="/about">Sobre</a>
            <a href="/receitas"><b>Receitas</b></a>
{% endblock header %}

{% block content %}
    <section class="cards-receitas">
{% for itens in item %}    
        <div class="card-receitas">
            <img src="{{itens.image}}" alt="imagem de {{itens.title}}">
            <p class="card-title">{{itens.title}}</p>
            <p class="card-subtitle">{{itens.author}}</p>
                </div>
            </div>
        </div>
 {% endfor %}
    </section>

    <section class="cards-receitas">
{% for itens in item %}    
        <div class="card-receitas">
            <img src="{{itens.image}}" alt="imagem de {{itens.title}}">
            <p class="card-title">{{itens.title}}</p>
            <p class="card-subtitle">{{itens.author}}</p>
                </div>
            </div>
        </div>
 {% endfor %}
    </section>

    
    <script src="/scripts/scripts.js"></script>
{% endblock content %}
